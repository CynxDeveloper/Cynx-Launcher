<Page x:Class="SusanooLauncher.Pages.GlobalChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Background="Transparent">

    <Grid Margin="28,22,28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Global Chat" FontSize="26" FontWeight="SemiBold" Foreground="#F2FFFFFF" />
            <TextBlock x:Name="StatusText" Margin="0,6,0,12" Foreground="#99FFFFFF" FontSize="13"
                       Text="Talk with everyone on the backend." />
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="14" Background="#22000000" Padding="12">
            <ScrollViewer x:Name="ChatScroll" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="MessagesPanel" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox x:Name="ChatInputBox" PlaceholderText="Type a message..." Height="40"
                        KeyDown="MessageKeyDown" />
            <ui:Button Grid.Column="1" Content="Send" Margin="8,0,0,0" Height="40" Width="80" Click="SendClicked"
                       Appearance="Primary" />
        </Grid>
    </Grid>
</Page>
