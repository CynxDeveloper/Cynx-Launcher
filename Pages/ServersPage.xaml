<Page x:Class="SusanooLauncher.Pages.ServersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent">
        <StackPanel Margin="28,22,28,22" MaxWidth="980">
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Servers" FontSize="26" FontWeight="SemiBold" Foreground="#F2FFFFFF" />
                <ui:Button Grid.Column="1" Width="120" Height="36" Content="Refresh" Click="RefreshClicked" />
            </Grid>

            <TextBlock x:Name="SummaryText" Margin="0,0,0,12" FontSize="24" FontWeight="SemiBold" Foreground="#FFFFFFFF" />

            <Border CornerRadius="16" Background="#22000000" Padding="18,8,18,8">
                <StackPanel>
                    <ItemsControl x:Name="SessionsList" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
